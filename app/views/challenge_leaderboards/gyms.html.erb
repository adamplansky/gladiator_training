<table class="table table-striped table-hover">
  <tr>
    <th>Pořadí</th>
    <th>Body</th>
    <th>Gym</th>
  </tr>
  <% @data_set.each do |data| %>
    <tr>
      <td><%= data["rank"] %>.</td>
      <td><%= data["points"] %></td>
      <td><%= Gym.find(data["gym_id"]).name %></td>
    </tr>
  <% end %>
</table>
