

<div class="container">
  <div class='col-sm-12'>

    <p id="notice"><%= notice %></p>
    <h2>Celkem jsme získali: <%= @all_my_points %> bodů</h2>
    <table class="table table-striped table-hover">
      <tbody>
        <% @trainings.each do |training| %>
         <%= render training%>
        <% end %>
      </tbody>
    </table>
    <%= paginate @trainings %>

  </div>
</div>