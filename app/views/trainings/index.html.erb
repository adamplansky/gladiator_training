

<div class="container">
  <div class='col-sm-12'>
    <p id="notice"><%= notice %></p>
    <table class="table table-striped table-hover">
      <tbody>
        <% @trainings.each do |training| %>
         <%= render training %>
        <% end %>
      </tbody>
    </table>
    <%= will_paginate @trainings, renderer: BootstrapPagination::Rails, :previous_label => 'Předchozí', :next_label => 'Další' %>

    <%# paginate @trainings %>
  </div>
</div>
