<%= form_for(@reacttest) do |f| %>
  <% if @reacttest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reacttest.errors.count, "error") %> prohibited this reacttest from being saved:</h2>

      <ul>
      <% @reacttest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
