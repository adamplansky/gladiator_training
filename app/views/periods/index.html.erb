<div class="col-sm-12">
  <div class="col-md-6 col-md-offset-3">


  <table class='table table-striped table-hover'>
  <thead>
    <tr>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @periods.each do |period| %>
      <tr>
        <td><%= period.name %> (<%= period.time_from.to_date %> - <%= period.time_to.to_date %>)</td>
        <td><%= link_to 'Statistika', controller: :statistics, action: :index, period_id: period.id %></td>
        <% if admin_in? %>
        <!-- <td><%# link_to 'Show', period %></td>
        <td><%# link_to 'Edit', edit_period_path(period) %></td>
        <td><%# link_to 'Destroy', period, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if admin_in? %>
<%= link_to 'New Period', new_period_path %>
<% end %>
</div>
</div>
